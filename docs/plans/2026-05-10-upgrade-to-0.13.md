# Hermes Agent upgrade: 0.10.0 → 0.13.0

Tracking doc for upgrading our fork from upstream `v2026.4.16` (0.10.0) to `v2026.5.7` (0.13.0, "The Tenacity Release"). Spans multiple sessions — update the **Status** and **Session log** sections as work progresses.

## Status

- **Current branch base:** `upstream/v2026.5.7` (0.13.0) — rebase + verification complete
- **Target:** `upstream/v2026.5.7` (0.13.0)
- **Phase:** 2 — verification (complete); Phase 3 cleanup items optional
- **Last updated:** 2026-05-11

## Summary of what upstream added (0.11–0.13)

Covered across three releases (~2500 commits). Highlights relevant to our fork:

- **0.11 (Interface):** Transport ABC (Anthropic / ChatCompletions / Responses / Bedrock), native NVIDIA NIM plugin, Ink-based TUI, `/steer`, shell hooks, orchestrator subagent role, expanded plugin surface.
- **0.12 (Curator):** Autonomous `hermes curator`, `hermes -z` one-shot mode, LM Studio first-class, pluggable gateway platforms, `redaction.enabled` default flipped to off.
- **0.13 (Tenacity):** Multi-agent Kanban, `/goal` Ralph-loop, Checkpoints v2, session auto-resume, 8 P0 security fixes (redaction back ON by default, Discord role-scoping, WhatsApp strangers rejected, MCP OAuth TOCTOU), `no_agent` cron mode, `ProviderProfile` plugin ABC, `allowed_{channels,chats,rooms}` across platforms, `[[as_document]]` skill directive, `transform_llm_output` plugin hook.

## Fork inventory & per-commit decisions

Nine commits ahead of `upstream/main`, plus uncommitted WIP.

| Commit | Summary | Decision | Rationale |
|---|---|---|---|
| `0a1486e42` | feat(nvidia): NVIDIA NIM provider | **Drop** | 0.11 shipped `plugins/model-providers/nvidia/` plugin with same env/aliases. Re-add tiny patch afterward only if we still want `nim`/`nemotron` aliases and `NIM_API_KEY` env. |
| `6bdf13bc0` | feat(security): network egress allowlist | **Keep — conflict expected** | No upstream equivalent. Hooks in heavily-churned files (`mcp_tool.py`, `browser_tool.py`, `web_tools.py`, `config.py`). Watch `_config_version` bump (fork is 18→19). |
| `cd12f0e17` | feat(skills): personal-skills dir | **Keep — clean** | New path, no upstream convention. 0.13's `--external-dirs` / `/reload-skills` complement but don't replace. |
| `191daf6f3` | feat(docker): hardened security profile | **Keep — conflict expected** | Upstream added orthogonal `docker_run_as_host_user` / `_BASE_SECURITY_ARGS` in `tools/environments/docker.py`. Merge carefully. |
| `82821e6d1` | docs(migration): MCP + Obsidian config | **Keep — clean** | Pure addition to `docs/migration/`. |
| `0326c8cb1` | feat(briefing): launchd daily briefing | **Keep now, replace later** | 0.13 has `hermes cron` + `no_agent` mode; 0.12 has `hermes -z`. After upgrade, consider migrating scheduling to `hermes cron`, one-shot invocation to `hermes -z`. Telegram glue still fork-only. |
| `02e6ba1c8` | fix(briefing): nvm PATH + date anchor | **Keep** | Travels with briefing stack. |
| `963820c0d` | fix(adhd-checkin): non-interactive push | **Keep** | Travels with briefing stack. |
| `55c2d2dc7` | fix(health-check): scope + bash-4 syntax | **Keep** | Travels with briefing stack. |

**WIP committed 2026-05-10 (now part of the fork-only set above):**
- `492df5624` feat(security): MCP tool-call approval gate (tools/mcp_approval.py + tests + hook in mcp_tool.py). Like the network-policy commit, will conflict with upstream mcp_tool.py churn during rebase.
- `c8367d3be` fix(briefing): always run in fresh session, never resume user DM. Clean — touches fork-only files only.

**Total fork-only commits now: 11** (was 9 + 2 WIP commits above; the docs/plans commit is separate tracking overhead).

## Phased plan

### Phase 0 — Prep (complete)
- [x] Review uncommitted WIP, split into coherent commits, land on `main` (done 2026-05-10 — two commits: MCP approval gate, briefing session fix)
- [x] Confirm `upstream` remote has `v2026.5.7` tag fetched (done)
- [x] Create backup branch `pre-0.13-upgrade` from current `main` (points at `0315b580f`)
- [x] Decide rebase vs merge-commit strategy — went with interactive rebase, dropped NIM

### Phase 1 — Rebase (complete)
- [x] `git rebase --interactive --onto v2026.5.7 v2026.4.16 main` (13 commits, 1 dropped, 12 replayed)
- [x] **Drop** `0a1486e42` (NVIDIA NIM) during rebase
- [x] Resolve conflicts in expected hotspots:
  - [x] `hermes_cli/config.py` (auto-merged cleanly for docker; for network policy, took upstream's `_config_version` and fixed test expectation for auto-populated PROVIDER_REGISTRY rules)
  - [x] `tools/mcp_tool.py` (no conflict — upstream churn didn't overlap our approval-hook line range)
  - [x] `tools/environments/docker.py` (merged hardened profile + upstream's `run_as_host_user`/`_GOSU_CAP_ARGS` — both coexist, --user short-circuits the SETUID/SETGID caps)
  - [x] `tools/browser_tool.py` (auto-merged)
  - [x] `tools/web_tools.py` (auto-merged)
- [x] Regenerate `uv.lock` with `uv lock` — bumped hermes-agent 0.10.0 → 0.13.0

### Phase 2 — Verification (complete)
- [x] `hermes doctor` passes (config v23, all required packages; 5 pre-existing env issues unchanged)
- [x] Morning briefing launchd run produces Telegram output (delivered via `scripts/hermes-briefing.sh --skill morning-briefing`)
- [x] ADHD check-in push path works (delivered via `scripts/hermes-briefing.sh --skill adhd-founder-planner`; scheduled launchd job remains intentionally disabled)
- [x] MCP / Obsidian connectivity intact (MCP tool loads in doctor; briefing prompts exercised obsidian-mcp surface)
- [x] Network policy denies an off-allowlist host (negative test — off-list returns block dict with host/port/message, wildcard `*.anthropic.com` allows subdomains, call sites wired in `web_tools.py`/`browser_tool.py`/`mcp_tool.py`)
- [x] Hardened Docker profile launches + tests pass (34/34 unit tests; live container verified `--user 65534`, `--read-only` blocks writes to `/` and `/etc`, `/var/cache/app` custom-writable path works, seccomp denies `unshare(CLONE_NEWUSER)`)
- [x] Skills in `personal-skills/` still discovered (briefing ran `morning-briefing` + `adhd-founder-planner` skills, both fork-only)

### Phase 3 — Post-upgrade opportunistic cleanup (optional, separate commits)
- [x] ~~Add thin fork patch for `nim`/`nemotron` aliases + `NIM_API_KEY`~~ — N/A. Upstream 0.13 already ships `nim` / `nvidia-nim` / `build-nvidia` / `nemotron` aliases in `hermes_cli/providers.py`. Only outstanding piece was `NIM_API_KEY` as a synonym for `NVIDIA_API_KEY`; skipped since there's no configured NIM usage. Trivial to add if ever needed.
- [x] Migrate `scripts/hermes-briefing.sh` from `hermes chat -Q -q` to `hermes -z` (commit `6774bcb17` — dropped ~50 lines of manual chrome-stripping filter; `-z` already prints ONLY the final response)
- [x] Migrate launchd plist → `hermes cron` (verified 2026-05-12: one-shot test job fired and delivered to Telegram via gateway's in-process ticker; daily `"5 7 * * *"` job created as `morning-briefing` with `telegram:$CHAT_ID` delivery; plist renamed to `com.hermes.morning-briefing.plist.disabled` and unloaded from launchd). Note: required a gateway restart onto 0.13.0 code first — the running gateway was still the 0.10.0 binary from April, so `find_gateway_pids()` couldn't see it until after `hermes gateway restart`.
- [ ] Evaluate enabling `hermes curator` — check pin/protect semantics against `personal-skills/` first
- [x] Verify `redaction.enabled` (now ON by default in 0.13) doesn't mangle Obsidian content — PASS. Tested 2026-05-12 with realistic Obsidian note (wikilinks, frontmatter, tasks, tags, dates, phone, prose, code block): wikilinks/frontmatter/tasks/tags all preserved untouched; real secrets (GitHub PAT, Discord snowflake) correctly masked. One benign false positive on `MAX_TOKENS = 200000` in a Python code block (ENV-assignment regex matches `TOKENS` substring); cosmetic only. One pre-existing gap noted: Zoom `?pwd=...` query param isn't in `_SENSITIVE_QUERY_PARAMS` (has `password` but not `pwd`) — not a regression from this upgrade.
- [ ] Consider `allowed_chats` on the Telegram platform as a complement to our network policy

## Conflict-risk notes (from pre-upgrade analysis)

- `hermes_cli/config.py`: upstream changed +1737 lines between `v2026.4.16..v2026.5.7`. Our two fork commits (network policy, docker hardening) both touch it. Expect rewrites, not line-level conflicts.
- `tools/mcp_tool.py`: +1309 upstream. Both our network-policy hook and the approval-gate hook land here.
- `tools/browser_tool.py`: +1727 upstream. Network-policy hook landing site.
- `tools/environments/docker.py`: +71 upstream. Small but the surface is the exact same code paths we customized.
- Upstream's `_config_version` may have been bumped past 19. If so, our bump collides and we need to pick the next free number and migrate.

## Session log

Add a dated entry each session.

### 2026-05-10 — planning session
- Audited fork (9 commits + WIP)
- Pulled release notes for 0.11, 0.12, 0.13
- Identified NVIDIA NIM commit as redundant
- Drafted this doc
- Next up: Phase 0 prep

### 2026-05-10 — Phase 0 kickoff (same session)
- Reviewed uncommitted WIP (MCP approval gate + briefing session fix)
- Ran `tests/tools/test_mcp_approval.py` — 23 passed
- Committed as two separate changes:
  - `492df5624` feat(security): MCP tool-call approval gate
  - `c8367d3be` fix(briefing): always run in fresh session, never resume user DM
- Committed tracking doc itself as `3e4f441d8`
- Tree clean, fork-only commit count is now 11 (12 if you count the docs/plans commit)
- Next up: create `pre-0.13-upgrade` backup branch, decide rebase vs merge-commit strategy, start Phase 1

### 2026-05-11 — Phase 1 rebase (multi-session)

**Session A — started rebase, applied 2/13:**
- Created backup branch `pre-0.13-upgrade` at `0315b580f`
- Stashed uv.lock (WIP from earlier commits didn't include a regen)
- Started `git rebase -i --onto v2026.5.7 v2026.4.16` with NVIDIA NIM commit marked `drop`
- Applied network-policy commit (`052f0da0a`) — resolved 4-file conflict. Key lesson: upstream's `PROVIDER_REGISTRY` grew from ~5 → 30+ entries, so `auto_allow_providers=True` now populates rules; had to filter `source.startswith("auto:")` in `test_load_network_allowlist_missing_section_returns_defaults`.
- Applied personal-skills commit (`26f681328`) — clean
- Paused before docker commit due to context pressure; memory file saved with full resume state

**Session B — finished rebase:**
- Resumed with context from `project_upgrade_0_13.md`
- Applied docker hardening (`6d4de829a`): 9 conflict hunks across 3 files
  - `tools/environments/docker.py` — interleaved our `security_profile`/`read_only_root`/`user`/`seccomp_profile`/`writable_paths` kwargs with upstream's `run_as_host_user`. Unified user-selection: host-user wins over hardened.user when both set; SETUID/SETGID caps dropped whenever any `--user` is in play (simpler than upstream's run_as_host_user-specific logic). Replaced upstream's `_HARDENED_SECURITY_ARGS` constant with an inline `+ ["--read-only"]` append to keep one code path.
  - `tools/terminal_tool.py` — concatenated both sets of config keys (all orthogonal)
  - `tests/tools/test_docker_environment.py` — concatenated both test suites
  - 34/34 tests pass
- Applied MCP+Obsidian docs (`13ec5ef1d`) — clean
- Applied briefing stack (4 commits: `482ec1bee`, `f10ba7098`, `9104ce0ed`, `7839424d9`). `482ec1bee` hit a `uv.lock` conflict — took `--theirs` (regenerating at end anyway).
- Applied MCP approval gate (`da8e1775b`) — **clean, no conflict** in `tools/mcp_tool.py`! Our hook at line 2211 didn't overlap upstream's churn regions. Note to planning doc: `mcp_tool.py` conflict anticipated but didn't materialize.
- Applied briefing session fix (`537a29fed`) — clean
- Applied tracking doc commits (`89f448714`, `738a6364f`) — clean
- Popped stash: only `uv.lock` conflicted (expected); other files were already part of replayed commits. Dropped stash after verifying.
- Regenerated `uv.lock` from scratch: `hermes-agent 0.10.0 → 0.13.0` confirmed
- Final HEAD: `738a6364f` with 12 fork commits on `498bfc7bc` (v2026.5.7)
- Next: Phase 2 verification

### 2026-05-11 — Phase 2 verification (same session, post-rebase)
- Committed uv.lock + plan doc updates from the rebase session (`d6a0eb61c`, `90ab28ca6`).
- **hermes doctor**: PASS. Config v23, all required Python packages present, all tool types register. 5 flagged issues are all pre-existing env state (missing symlink, boto3, tinker submodule, Gemini key, setup API keys) — none are regressions from the upgrade. One misleading warning ("python-telegram-bot not installed") was environment drift: `uv sync` resets the venv to core deps only, so the `[messaging]` extra (which briefing + gateway need for Telegram) had to be re-installed with `uv sync --extra dev --extra messaging`.
- **Morning briefing**: PASS. `scripts/hermes-briefing.sh --skill morning-briefing` delivered to chat 7236389286 in ~1m20s, status file `{"status":"ok"}` written. Fresh-session fix (`537a29fed`) and nvm PATH + date anchor fix (`f10ba7098`) both intact.
- **ADHD push**: PASS. Same script with `--skill adhd-founder-planner` delivered afternoon wind-down message. Scheduled launchd job `com.hermes.adhd-checkin.plist.disabled` left disabled per prior preference.
- **Network policy**: PASS. Ran `tests/tools/test_network_policy.py` (20/20) and a live positive+negative integration test via temp YAML config. `api.anthropic.com` (on list) allowed; `evil.example.com` and `staging.anthropic.com` (off list with bare pattern) both blocked with full metadata; `*.anthropic.com` wildcard correctly allows subdomains while still rejecting unrelated hosts. All three call sites — `web_tools.py` (5 hits), `browser_tool.py` (1 hit), `mcp_tool.py` (1 hit) — confirmed intact post-rebase.
- **Hardened Docker**: PASS. `tests/tools/test_docker_environment.py` 34/34. Live smoke test with `security_profile="hardened"`, `user="65534:65534"`, `seccomp_profile=docker/hardened-profile/seccomp-default.json`, `writable_paths=["/var/cache/app"]` and `python:3.11-slim` image confirmed: container runs as `uid=65534(nobody)`, writes to `/tmp` and `/var/cache/app` succeed, writes to `/` and `/etc` fail with "Read-only file system", `unshare(CLONE_NEWUSER)` returns EPERM (seccomp-blocked).
- **Phase 2 done. Phase 3 items are optional; revisit as appetite permits.**

### 2026-05-12 — Phase 3 opportunistic cleanup (partial)
- **Redaction / Obsidian safety**: verified redactor leaves wikilinks, frontmatter, Obsidian task syntax, tags, dates, prose, and phone numbers untouched. Masked real secrets (GitHub PAT, Discord snowflake). One cosmetic false positive noted; one pre-existing Zoom `?pwd=` gap flagged (not a regression).
- **Briefing → `hermes -z`** (commit `6774bcb17`): swapped `hermes chat -Q -q --source tool` for `hermes -z`. Dropped the embedded Python filter (~50 lines) that was manually reproducing what -z now provides for free (banner/spinner/session_id/approval-prompt suppression). Live-tested via `scripts/hermes-briefing.sh --skill morning-briefing` → Telegram delivery confirmed.
- **launchd plist → `hermes cron`**: required a gateway restart onto 0.13.0 code first (`hermes gateway restart` — the running gateway was the pre-upgrade 0.10.0 binary from 26-Apr, invisible to `find_gateway_pids()`). One-shot test cron job with `--deliver telegram:$CHAT_ID` fired correctly via the gateway's 60s in-process ticker and delivered end-to-end. Replaced the launchd plist with a persistent daily job: `"5 7 * * *"` → `morning-briefing` skill → `telegram:$CHAT_ID`. `com.hermes.morning-briefing.plist` unloaded and renamed to `.disabled`. The `scripts/hermes-briefing.sh` + health-check plist remain in place as fallback infrastructure.
- **NIM aliases patch**: skipped — upstream 0.13 already ships `nim`/`nvidia-nim`/`build-nvidia`/`nemotron` aliases in `hermes_cli/providers.py`. Only missing piece would be `NIM_API_KEY` as a synonym for `NVIDIA_API_KEY`, which isn't currently used.
- **Still open (Phase 3)**: `hermes curator` evaluation against `personal-skills/`, `allowed_chats` on Telegram platform as a network-policy complement.
